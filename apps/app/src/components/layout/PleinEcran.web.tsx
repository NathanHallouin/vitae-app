/**
 * Sur le web, il n'y a pas de barre système à masquer : le navigateur décide de son propre cadre,
 * et une page n'a pas à en disposer.
 *
 * La variante native porte le geste, et son commentaire explique pourquoi c'est un composant et
 * non un appel dans un effet.
 */
export default function PleinEcran() {
  return null;
}
