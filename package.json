{
  "name": "vitae",
  "version": "0.1.0",
  "private": true,
  "description": "Calculateur de métabolisme de base : site Next.js et application native Expo, métier partagé",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd apps/web dev",
    "dev:mobile": "bun run --cwd apps/mobile start",
    "content": "bun run --cwd packages/content build",
    "tokens": "bun run tools/build-tokens.ts",
    "build": "bun run tokens && bun run content && bun run --cwd apps/web build",
    "build:mobile": "bun run tokens && bun run content && bun run --cwd apps/mobile prebuild",
    "start": "bun run --cwd apps/web start",
    "test": "bun test packages/core",
    "check": "biome check",
    "check:fix": "biome check --write",
    "format": "biome format --write",
    "typecheck": "bun run --cwd apps/web typecheck && bun run --cwd apps/mobile typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.7",
    "@types/bun": "^1.3.14",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "@react-native-community/datetimepicker": "9.1.0"
  }
}
